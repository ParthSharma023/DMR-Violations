# PBIX Research — index

Early notes from the first extraction pass. **Most content here is superseded by dedicated docs.** Pointers:

## Where to find things

- **PBIX model docs** (DAX measures, calc tables, M queries, relationships, violation rule deep-dive): `~/Documents/CoH/context/pbix/` — entry point `00_overview.md` (14 files)
- **PBIX layout docs** (all 104 pages, every visual's field bindings, bookmarks, filters): same folder, files `10_pages_index.md` through `14_filters.md`
- **v2 app docs** (what we built, how it works): `docs/` in this project. Start at `docs/ARCHITECTURE.md`
- **Current state snapshot**: `AUDIT.md` (auto-generated by `python tools/audit_pages.py`)
- **Master README**: `README.md` in project root
- **Claude-specific onboarding**: `CLAUDE.md` in project root

## Key facts that stabilized from extraction

- PBIX is **Import mode** with a 452 MB uncompressed VertiPaq model embedded. Upstream source is a Power BI Dataflow, workspace `597882e1-04dd-4e9b-bf59-bceae3a2043b`, primary dataflow `44802b8e-bfe1-45d5-9acb-a23dac5ed9e6`.
- **58 live pages** of 104 total (5 visible tabs + 53 reached via bookmarks from the Home navigation hub).
- **Global filters on every page**: `VARDESC.WWTP NOT IN (...)` with 11 excluded values, `DATATBL.DATESTAMP >= 2005-01-01`.
- **The exported CSVs in `source/exported_csv/` are post-DAX** — `DATATBL.csv` already has `Violation`, `Limit`, `Compare`, `Categories One`, `Color Format for Flow`, and every other calc column materialized.
